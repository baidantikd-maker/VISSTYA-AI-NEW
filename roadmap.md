here's where we are on the roadmap:

DONE
 Env var validation + key-failure isolation (done, now confirmed compiling)
 CORS via env var (done)
 Deployment target decided (Render) 

 TO BE DONE
 Client VITE_API_BASE_URL ← next
 Deploy backend to Render
 Vision/evidence real provider calls
 Auth/persistence direction (Supabase?)
 Rate limiting
 Frontend ↔ backend wiring
 EXIF extraction
 Tests for scoring.ts